<!-- MembersMapper (myBatis) -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.springchap4.mapper.MembersMapper">
	<!-- 회원등록 -->
	<insert id="insertMember" parameterType="com.kh.springchap4.model.Member">
	INSERT INTO Members (MemberID, USERNAME, PASSWORD, FULLNAME, EMAIL, REGISTRATIONDATE)
	VALUES (members_seq.NEXTVAL, #{username}, #{password}, #{fullName}, #{email}, sysdate)
	</insert>
	<select id="getAllMembers" resultType="com.kh.springchap4.model.Member">
	   SELECT * FROM Members
	</select>
	<!-- 아이디 1개 조회 -->
	<select id="getMemberById" parameterType="long" resultType="com.kh.springchap4.model.Member"> 				 
		SELECT * FROM members WHERE MemberID = #{memberId}
	</select>
	
	 <!-- 사용자 정보 업데이트 -->
	<update id="updateMember" parameterType="com.kh.springchap4.model.Member">
		UPDATE Members
		SET
		USERNAME = #{username},
		PASSWORD = #{password},
		FULLNAME = #{fullName},
		EMAIL = #{email}
		WHERE MemberID = #{MemberID}
	</update>
	
</mapper>